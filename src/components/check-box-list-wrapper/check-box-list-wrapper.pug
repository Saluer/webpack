include ../label-wrapper/label-wrapper.pug
include ./__check-box-list/check-box-list-wrapper__check-box-list.pug
mixin checkBoxListWrapper(options)
    .check-box-list-wrapper
        +labelWrapper({labelText:options.label})
        +checkBoxListWrapper__checkBoxList(options)