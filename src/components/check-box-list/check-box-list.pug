include ../check-box-wrapper/check-box-wrapper.pug

mixin checkBoxList(options)
    .check-box-list
        -for(var checkBoxData of options.checkBoxesDataCollection)
            +checkBoxWrapper(checkBoxData)